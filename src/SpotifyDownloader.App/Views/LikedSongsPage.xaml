<Page
    x:Class="SpotifyDownloader.App.Views.LikedSongsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:core="using:SpotifyDownloader.Core.Models">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="16" Margin="0,0,0,8">
            <TextBlock Text="Músicas Curtidas" Style="{StaticResource TitleTextBlockStyle}" VerticalAlignment="Center"/>
            <Button Content="Baixar Todas" Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Center"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="{x:Bind ViewModel.LikedCountText, Mode=OneWay}" 
                   Style="{StaticResource BodyTextBlockStyle}"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,0,12"/>

        <ScrollViewer Grid.Row="2">
            <muxc:ItemsRepeater ItemsSource="{x:Bind ViewModel.LikedTracks, Mode=OneWay}">
                <muxc:ItemsRepeater.Layout><StackLayout Orientation="Vertical"/></muxc:ItemsRepeater.Layout>
                <muxc:ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="core:SpotifyTrack">
                        <Grid Padding="8,4" CornerRadius="6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="48"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Image Source="{x:Bind AlbumCoverUrl}" Width="48" Height="48" CornerRadius="4"/>
                            <StackPanel Grid.Column="1" Margin="12,0" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <TextBlock Text="{x:Bind Title}" Style="{StaticResource BodyStrongTextBlockStyle}" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{x:Bind ExplicitLabel}" Style="{StaticResource CaptionTextBlockStyle}" 
                                               Foreground="{ThemeResource SystemAccentColor}" 
                                               Visibility="{x:Bind IsExplicit, Mode=OneWay}"/>
                                </StackPanel>
                                <TextBlock Text="{x:Bind Artist}" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </StackPanel>
                            <TextBlock Grid.Column="2" Text="{x:Bind DurationFormatted}" VerticalAlignment="Center"
                                       HorizontalAlignment="Center" Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                            <Button Grid.Column="3" FontFamily="Segoe MDL2 Assets" Content="&#xE768;" 
                                    Background="Transparent" BorderThickness="0" Width="36" Height="36"/>
                            <Button Grid.Column="4" Content="Baixar" Style="{StaticResource AccentButtonStyle}" Margin="4,0,0,0"/>
                        </Grid>
                    </DataTemplate>
                </muxc:ItemsRepeater.ItemTemplate>
            </muxc:ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
